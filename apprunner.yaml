version: 1.0
runtime: python3.11
build:
  commands:
    - python -m ensurepip
    - python -m pip install --no-cache-dir --upgrade pip
    - pip install -r requirements.txt
run:
  command: python app.py
